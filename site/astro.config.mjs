// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://vibestats.app',
  trailingSlash: 'never',
  compressHTML: false,
});
